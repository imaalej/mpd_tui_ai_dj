numpy>=1.24.0
urwid>=2.1.0
Pillow>=9.0.0
mutagen>=1.46.0  # Required: read audio tags directly for queue metadata display

# Phase 3 - CLAP Embeddings (optional, only needed for generation)
# Install with: pip install transformers torch torchaudio --break-system-packages
# transformers>=4.30.0
# torch>=2.0.0
# torchaudio>=2.0.0
